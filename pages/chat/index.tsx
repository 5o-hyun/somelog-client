import ChatContainer from '@container/chat/ChatContainer';

import Layout from '@components/base/Layout';

import React from 'react';

const index = () => {
  return <Layout>{/* <ChatContainer /> */}로그인 오류로인한 임시처리</Layout>;
};

export default index;
