import DiaryCreateContainer from '@container/diary/DiaryCreateContainer';

import Layout from '@components/base/Layout';

import React from 'react';

const create = () => {
  return (
    <Layout>
      <DiaryCreateContainer />
    </Layout>
  );
};

export default create;
