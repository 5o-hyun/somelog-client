import MemoCreateContainer from '@container/memo/MemoCreateContainer';

import Layout from '@components/base/Layout';

import React from 'react';

const create = () => {
  return (
    <Layout>
      <MemoCreateContainer />
    </Layout>
  );
};

export default create;
